<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ti.dam.mobilee_learning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".StudentActivity"
            android:theme="@style/student" />
        <activity
            android:name=".TeacherActivity"
            android:theme="@style/student" />
        <activity android:name=".GuestActivity" />
        <activity android:name=".ChooseSpecialiteActivity" />
        <activity
            android:name=".ModulesActivity"
            android:label="Modules" />
        <activity
            android:name=".CourActivity"
            android:theme="@style/student" />
        <activity android:name=".CommentDialog" />
        <activity android:name=".ChooseTestActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".ChooseModuleActivity" />
        <activity
            android:name=".AddCourseActivity"
            android:label="@string/title_activity_add_course" />
        <activity android:name=".InboxActivity"></activity>
    </application>

</manifest>